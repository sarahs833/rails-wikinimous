
<% @articles.each do |article| %>
<h2><%= link_to article.title, article_path(article.id) %></h2>
<p><%= article.content %></p>
<% end %>

<button><%= link_to "Create new", new_article_path %></button>
